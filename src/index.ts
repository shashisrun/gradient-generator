(function () {
  const body = document.body;

  const title: HTMLHeadingElement = document.createElement("h1");

  title.textContent = "Hello CSS Gradient Generator";

  body.appendChild(title);
})();
